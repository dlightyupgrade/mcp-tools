/**
 * Basic MCP Tools
 * 
 * Fundamental tools for MCP server functionality testing and debugging
 */

import { Server } from '@modelcontextprotocol/sdk/server/index.js';

export function setupBasicTools(server: Server): void {
  // Tools are registered in the main server.ts file
  // This file can be extended with additional basic tool implementations
  
  console.error('✅ Basic tools registered');
}